// Chapter 3: Mirror Maze
// Sliding Window & Two Pointers

package main

// MaxSumSubarray finds the maximum sum of a subarray of size k using sliding window
func MaxSumSubarray(arr []int, k int) int {
	// TODO: Find maximum sum of subarray of size k using sliding window
	// Return the maximum sum
	return 0
}

// TwoSum finds two indices where arr[i] + arr[j] = target
// Array is sorted. Use two pointers approach.
// Returns slice of two indices [i, j] or nil if no solution
func TwoSum(arr []int, target int) []int {
	// TODO: Find two indices where arr[i] + arr[j] = target
	// Array is sorted. Use two pointers approach.
	// Return []int{i, j} or nil if no solution
	return nil
}

// ThreeSum finds all unique triplets that sum to target
// Returns a slice of triplets [][]int{{a, b, c}, ...}
func ThreeSum(arr []int, target int) [][]int {
	// TODO: Find all unique triplets that sum to target
	// Return slice of triplets [][]int{{a, b, c}, ...}
	return [][]int{}
}
