// Chapter 3: Mirror Maze
// Sliding Window & Two Pointers

export function maxSumSubarray(arr: number[], k: number): number {
  // TODO: Find maximum sum of subarray of size k using sliding window
  // Return the maximum sum
  return 0;
}

export function twoSum(arr: number[], target: number): [number, number] | null {
  // TODO: Find two indices where arr[i] + arr[j] = target
  // Array is sorted. Use two pointers approach.
  // Return [i, j] or null if no solution
  return null;
}

export function threeSum(arr: number[], target: number): number[][] {
  // TODO: Find all unique triplets that sum to target
  // Return array of triplets [[a, b, c], ...]
  return [];
}
